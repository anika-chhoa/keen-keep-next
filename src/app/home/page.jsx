export const metadata = {
  title: "keen-keep | Home",
  description: "All friends",
};

const Homepage = () => {
    return (
        <div>
            homepage
        </div>
    );
};

export default Homepage;