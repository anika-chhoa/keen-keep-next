export const metadata = {
  title: "keen-keep | Stats",
  description: "Friends stats",
};

const Stats = () => {
    return (
        <div>
            Stats
        </div>
    );
};

export default Stats;