export const metadata = {
  title: "keen-keep | Timeline",
  description: "Friends timeline",
};

const Timeline = () => {
    return (
        <div>
            timeline
        </div>
    );
};

export default Timeline;